This directory will contain Alembic revision files created by running:

    alembic revision --autogenerate -m "create initial"

After creating revisions, run migrations with:

    alembic upgrade head
